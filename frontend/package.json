{
  "name": "no-register-chat-frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "node -e \"const fs=require('fs'); if(!fs.existsSync('dist')) fs.mkdirSync('dist'); fs.copyFileSync('src/embed.js', 'dist/embed.iife.js');\"",
    "build:vite": "vite build",
    "preview": "vite preview --port 5173"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
